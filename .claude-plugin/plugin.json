{
  "name": "omatic-server",
  "version": "1.3.3",
  "description": "O-Matic Server connection — bundled PostgreSQL + pgvector MCP server for the O-Matic factory brain. Ships Probot (orchestrator), Fred (storage + connection CRUD), and Data (analyst + DBA) skills. Replaces the hand-edited omatic-server block in claude_desktop_config.json.",
  "author": {
    "name": "James Walker",
    "url": "https://o-matic.io"
  },
  "homepage": "https://o-matic.io",
  "keywords": ["omatic", "factory", "postgres", "pgvector", "mcp", "brain", "probot", "fred", "data"],
  "skills": "./skills/",
  "mcpServers": {
    "omatic-server": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.js"],
      "env": {
        "OMATIC_PROJECT_ROOT": "${CLAUDE_PROJECT_DIR}",
        "OMATIC_FACTORY_JSON_PATH": "${CLAUDE_PROJECT_DIR}/.omatic/factory.json",
        "OMATIC_PLATFORM": "claude-code"
      }
    }
  }
}
